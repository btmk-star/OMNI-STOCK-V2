import { PageStub } from '@/components/shared/page-stub';

export default function DeliveryPage() {
  return (
    <PageStub
      title="Delivery"
      subtitle="Track incoming stock — pending / in-transit / arrived / inspected / accepted"
      phase="Phase 3"
    />
  );
}
