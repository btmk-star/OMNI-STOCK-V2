import { PageStub } from '@/components/shared/page-stub';

export default function UsersPage() {
  return (
    <PageStub
      title="Users"
      subtitle="User management — role-based (admin only)"
      phase="Phase 6"
    />
  );
}
