import { PageStub } from '@/components/shared/page-stub';

export default function BillingPage() {
  return (
    <PageStub
      title="Billing"
      subtitle="Transaksi PO yang sudah completed — status PAID / UNPAID, export Excel"
      phase="Phase 3"
    />
  );
}
