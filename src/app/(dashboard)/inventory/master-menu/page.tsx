import { PageStub } from '@/components/shared/page-stub';

export default function MasterMenuPage() {
  return (
    <PageStub
      title="Master Menu"
      subtitle="172 menu final — linked ke Pawoon product via pawoon_product_id"
      phase="Phase 3"
    />
  );
}
