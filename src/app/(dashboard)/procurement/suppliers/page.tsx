import { PageStub } from '@/components/shared/page-stub';

export default function SuppliersPage() {
  return (
    <PageStub
      title="Suppliers"
      subtitle="Master supplier (kerjasama / non-kerjasama / online shop) — rating + price comparison"
      phase="Phase 3"
    />
  );
}
