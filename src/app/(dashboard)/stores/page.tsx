import { PageStub } from '@/components/shared/page-stub';

export default function StoresPage() {
  return (
    <PageStub
      title="Stores"
      subtitle="Manage outlet — enriched dari pawoon_outlets"
      phase="Phase 6"
    />
  );
}
