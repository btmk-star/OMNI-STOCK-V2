import { PageStub } from '@/components/shared/page-stub';

export default function SettingsPage() {
  return (
    <PageStub
      title="Settings"
      subtitle="Admin config — API keys, sync settings, system configuration"
      phase="Phase 6"
    />
  );
}
