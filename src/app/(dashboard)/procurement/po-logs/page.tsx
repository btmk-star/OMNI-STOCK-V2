import { PageStub } from '@/components/shared/page-stub';

export default function POLogsPage() {
  return (
    <PageStub
      title="PO Logs"
      subtitle="Full audit trail per PO — siapa, kapan, status berubah dari/ke apa"
      phase="Phase 3"
    />
  );
}
