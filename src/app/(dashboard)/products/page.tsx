import { PageStub } from '@/components/shared/page-stub';

export default function ProductsPage() {
  return (
    <PageStub
      title="Products"
      subtitle="Produk, kategori, dan harga — auto-sync dari Pawoon"
      phase="Phase 2"
    />
  );
}
