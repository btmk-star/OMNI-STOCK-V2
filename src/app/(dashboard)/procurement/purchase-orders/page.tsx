import { PageStub } from '@/components/shared/page-stub';

export default function PurchaseOrdersPage() {
  return (
    <PageStub
      title="Purchase Orders"
      subtitle="Create & manage PO — approval flow + WA template generator"
      phase="Phase 3"
    />
  );
}
