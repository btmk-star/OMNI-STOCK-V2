import { PageStub } from '@/components/shared/page-stub';

export default function ReportsPage() {
  return (
    <PageStub
      title="Reports"
      subtitle="Daily / Weekly / Monthly reports — sales summary, COGS, margin trends, session breakdown"
      phase="Phase 4"
    />
  );
}
